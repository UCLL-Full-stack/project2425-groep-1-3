import React from "react";
import { useRouter } from "next/router";

type Props = {};

const AddCarPart: React.FC<Props> = ({}: Props) => {
  const router = useRouter();

  return <>
  {

  }</>;
};
export default AddCarPart;
